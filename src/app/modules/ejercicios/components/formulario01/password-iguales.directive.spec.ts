import { PasswordIgualesDirective } from './password-iguales.directive';

describe('PasswordIgualesDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordIgualesDirective();
    expect(directive).toBeTruthy();
  });
});
